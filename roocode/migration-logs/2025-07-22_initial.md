# Migration Record — mattpocock/skills → Roo/Zoo Code

## Source Revision

- **Source repo**: `mattpocock/skills`
- **Git rev**: `f57076b2e9ee812c5a7d37432798d009e423e15a`

---

## Migration Rules

以下规则指导了本次迁移的所有决策：

### 格式规则

| 规则 | 说明 |
|---|---|
| **R1 — Slash Command 格式** | 目标文件置于 `roocode/commands/`，单个 `.md` 文件，`---\ndescription: "..."\n---` 格式，无 `name` 字段、无 `disable-model-invocation`。 |
| **R2 — Skill 格式** | 目标文件置于 `roocode/skills/<name>/SKILL.md`，可附带同级子文件。保留 frontmatter 中的 `name` 和 `description`，无 `disable-model-invocation`。 |
| **R3 — Slash Command 不支持子文件引用** | Slash Command 是单个 `.md`，所有原 skill 引用的子文件内容必须内联到该 `.md` 中。 |
| **R4 — Slash Command 不支持脚本** | 原 skill 的 `scripts/` 目录不能在 slash command 中使用。但 skill 中保留引用不受影响。 |
| **R5 — Skill 不支持 disable-model-invocation** | Roo/Zoo 的 skill 全部为 model-invoked，无法阻止 Agent 自动触发。原 user-invoked skill 转为 slash command 或接受此行为。 |

### 引用规则

| 规则 | 说明 |
|---|---|
| **R6 — Slash Command 引用其他 Slash Command** | 不可"自动调用"另一个 slash command。原文中的"run `/xxx`"改为"告诉用户运行 `/xxx`"或"建议用户运行 `/xxx`"。 |
| **R7 — Slash Command 引用 Skill** | 保持不变，Skill 名称在 Agent 上下文中可见，Agent 可以自动调用。原文的 `/foo` 改为 `foo` skill。 |
| **R8 — Skill 引用 Slash Command** | 不能"hand off to /xxx"，因为 Agent 看不到 slash command。改为"建议用户运行 `/xxx`"。 |
| **R9 — Skill 引用 Skill** | 保持不变。 |

### 迁移范围规则

| 规则 | 说明 |
|---|---|
| **R10 — 排除 personal** | `skills/personal/` 下所有内容不迁移（Matt 个人使用）。 |
| **R11 — 排除 in-progress** | `skills/in-progress/` 下所有内容不迁移。 |
| **R12 — 排除 deprecated** | 已废弃 skill 不迁移。 |
| **R13 — 按需排除** | `git-guardrails-claude-code`（Claude Code 专用）、`setup-pre-commit`、`migrate-to-shoehorn`、`scaffold-exercises` 不迁移。`agents/openai.yaml` 文件不迁移（Cline 系平台不使用此 Codex 元数据文件）。 |

---

## 迁移清单

### Slash Commands（13 个）

目标：`roocode/commands/<name>.md`

#### 1. grill-me
- **来源**: `skills/productivity/grill-me/SKILL.md`
- **类型**: User-invoked → Slash Command
- **修改**：
  - 去掉 `name`、`disable-model-invocation: true`，改为 `description` 格式（R1）
  - 原文 `Run a /grilling session.` 改为 `Run a grilling session using the 'grilling' skill.`（R7）

#### 2. grill-with-docs
- **来源**: `skills/engineering/grill-with-docs/SKILL.md`
- **类型**: User-invoked → Slash Command
- **修改**：
  - 去掉 `name`、`disable-model-invocation: true`，改为 `description` 格式（R1）
  - 原文 `Run a /grilling session, using the /domain-modeling skill.` 改为 `Run a grilling session using the 'grilling' skill, combined with the 'domain-modeling' skill...`（R7）

#### 3. handoff
- **来源**: `skills/productivity/handoff/SKILL.md`
- **类型**: User-invoked → Slash Command
- **修改**：
  - 去掉 `name`、`disable-model-invocation: true`、`argument-hint`，改为 `description` 格式（R1）
  - `description` 中合并了 argument-hint 信息
  - `run /setup-matt-pocock-skills if the table is missing` → `tell the user to run /setup-matt-pocock-skills if the table is missing`（R6）

#### 4. implement
- **来源**: `skills/engineering/implement/SKILL.md`
- **类型**: User-invoked → Slash Command
- **修改**：
  - 去掉 `name`、`disable-model-invocation: true`，改为 `description` 格式（R1）
  - `Use /tdd` → `Use the 'tdd' skill`（R7）
  - `/code-review` 引用保持不变——`implement` 输出给用户的信息中本就是指示用户运行 `/code-review`，属于 R6 的自然形态

#### 5. ask-matt
- **来源**: `skills/engineering/ask-matt/SKILL.md`
- **类型**: User-invoked → Slash Command
- **修改**：
  - 去掉 `name`、`disable-model-invocation: true`、`# Ask Matt` 标题，改为 `description` 格式（R1）
  - `description` 措辞从 "A router over the skills in this repo" 改为 "A router over the slash commands and skills in this repo"
  - 所有 slash command 引用改为告诉用户调用句式（R6）：`/grill-with-docs` 等保持不变（这些是用户输入的 slash command 名称，保留 `/` 前缀）；但描述 Agent 自动触发行为的引用改为 skill 名称
  - `/prototype` → `prototype` skill（R7：prototype 是 model-invoked）
  - `/diagnosing-bugs` → `diagnosing-bugs` skill（R7）
  - `/domain-modeling` → `domain-modeling` skill（R7）
  - `/codebase-design` → `codebase-design` skill（R7）
  - `/tdd` → `tdd` skill（R7）
  - `/research` → `research` skill（R7）
  - `hand off to /improve-codebase-architecture` → `suggest the user run /improve-codebase-architecture`（R6）

#### 6. to-spec
- **来源**: `skills/engineering/to-spec/SKILL.md`
- **类型**: User-invoked → Slash Command
- **修改**：
  - 去掉 `name`、`disable-model-invocation: true`，改为 `description` 格式（R1）
  - `run /setup-matt-pocock-skills if not` → `tell the user to run /setup-matt-pocock-skills if not`（R6）

#### 7. to-tickets
- **来源**: `skills/engineering/to-tickets/SKILL.md`
- **类型**: User-invoked → Slash Command
- **修改**：
  - 去掉 `name`、`disable-model-invocation: true`、`# To Tickets` 标题，改为 `description` 格式（R1）
  - `run /setup-matt-pocock-skills if not` → `tell the user to run /setup-matt-pocock-skills if not`（R6）
  - `Work the frontier one ticket at a time with /implement` → `Tell the user to work the frontier one ticket at a time with /implement`（R6）

#### 8. triage（内联 2 个子文件）
- **来源**: `skills/engineering/triage/SKILL.md` + `AGENT-BRIEF.md` + `OUT-OF-SCOPE.md`
- **类型**: User-invoked → Slash Command
- **修改**：
  - 去掉 `name`、`disable-model-invocation: true`、`# Triage` 标题，改为 `description` 格式（R1）
  - 将 `AGENT-BRIEF.md`（208 行）和 `OUT-OF-SCOPE.md`（106 行）内联为 `# Agent Briefs` 和 `# Out-of-Scope Knowledge Base` 节（R3）
  - `run /setup-matt-pocock-skills if not` → `tell the user to run /setup-matt-pocock-skills if not`（R6）
  - `Optionally run /code-review` → `Optionally tell the user to run /code-review`（R6）
  - `run the /grilling and /domain-modeling skills` → `run the grilling and domain-modeling skills`（R7）

#### 9. wayfinder
- **来源**: `skills/engineering/wayfinder/SKILL.md`
- **类型**: User-invoked → Slash Command
- **修改**：
  - 去掉 `name`、`disable-model-invocation: true`，改为 `description` 格式（R1）
  - `run /setup-matt-pocock-skills if not` → `tell the user to run /setup-matt-pocock-skills if not`（R6）
  - `Run a /grilling and /domain-modeling session` → `Run a grilling and domain-modeling session`（R7）
  - `spin up a /research subagent` → `spin up a research subagent`（R7）
  - `UI/logic code via the /prototype skill` → `UI/logic code via the prototype skill`（R7）
  - `the /grilling and /domain-modeling skills` → `the grilling and domain-modeling skills`（R7）
  - `Run /to-spec to synthesize` → `Tell the user to run /to-spec to synthesize`（R6）
  - `then /to-tickets to split it` → `then tell the user to run /to-tickets to split it`（R6）
  - `Then tell the user to run /implement per ticket`（R6）

#### 10. improve-codebase-architecture（内联 1 个子文件）
- **来源**: `skills/engineering/improve-codebase-architecture/SKILL.md` + `HTML-REPORT.md`
- **类型**: User-invoked → Slash Command
- **修改**：
  - 去掉 `name`、`disable-model-invocation: true`、`# Improve Codebase Architecture` 标题，改为 `description` 格式（R1）
  - 将 `HTML-REPORT.md`（124 行）内联为 `# HTML Report Format` 节（R3）
  - `Run the /codebase-design skill` → `Use the codebase-design skill`（R7）
  - `the /codebase-design vocabulary` → `the codebase-design vocabulary`（R7）
  - `run the /grilling skill` → `run the grilling skill`（R7）
  - `run the /domain-modeling skill` → `run the domain-modeling skill`（R7）
  - `Run the /codebase-design skill and use its design-it-twice` → `Run the codebase-design skill and use its design-it-twice`（R7）

#### 11. setup-matt-pocock-skills（内联 5 个子文件）
- **来源**: `skills/engineering/setup-matt-pocock-skills/SKILL.md` + `domain.md` + `triage-labels.md` + `issue-tracker-github.md` + `issue-tracker-gitlab.md` + `issue-tracker-local.md`
- **类型**: User-invoked → Slash Command
- **修改**：
  - 去掉 `name`、`disable-model-invocation: true`、`# Setup Matt Pocock's Skills` 标题，改为 `description` 格式（R1）
  - 将 5 个子文件内联到 `# Seed Templates` 节下各分节（R3）：`## Domain Docs`、`## Triage Labels`、`## Issue Tracker: GitHub`、`## Issue Tracker: GitLab`、`## Issue Tracker: Local Markdown`
  - `domain.md` 中 `/domain-modeling` skill 引用（R7）：`/domain-modeling` → `domain-modeling` skill
  - `domain.md` 中 `/grill-with-docs` 和 `/improve-codebase-architecture` 保持不变——它们出现在说明性文本中，是 slash command 名称

#### 12. teach（内联 4 个子文件）
- **来源**: `skills/productivity/teach/SKILL.md` + `MISSION-FORMAT.md` + `RESOURCES-FORMAT.md` + `LEARNING-RECORD-FORMAT.md` + `GLOSSARY-FORMAT.md`
- **类型**: User-invoked → Slash Command
- **修改**：
  - 去掉 `name`、`disable-model-invocation: true`、`argument-hint`，改为 `description` 格式（R1）
  - `description` 中合并了 argument-hint 信息
  - 将 4 个子文件内联到末尾各分节（R3）：`# MISSION.md Format`、`# RESOURCES.md Format`、`# Learning Record Format`、`# GLOSSARY.md Format`
  - 子文件中的 `[SKILL.md](./SKILL.md)` 自引用链接移除，改为上下文内引用
  - `[[MISSION.md]]` 和 `[[GLOSSARY.md]]` 双链格式改为普通引用格式

#### 13. writing-great-skills（内联 1 个子文件）
- **来源**: `skills/productivity/writing-great-skills/SKILL.md` + `GLOSSARY.md`
- **类型**: User-invoked → Slash Command
- **修改**：
  - 去掉 `name`、`disable-model-invocation: true`，改为 `description` 格式（R1）
  - 将 `GLOSSARY.md`（202 行）内联到末尾，作为 `# Glossary — Building Great Skills` 节（R3）
  - `Bold terms are defined in [GLOSSARY.md](GLOSSARY.md)` → `Bold terms are defined in the Glossary section at the bottom`
  - `this skill discloses its full definitions to GLOSSARY.md` 保持不变（描述性文本）

---

### Skills（9 个）

目标：`roocode/skills/<name>/SKILL.md`，保留原始子文件结构

#### 14. grilling
- **来源**: `skills/productivity/grilling/SKILL.md`
- **类型**: Model-invoked → Skill
- **修改**：无。自包含 13 行，无引用需要修正。

#### 15. resolving-merge-conflicts
- **来源**: `skills/engineering/resolving-merge-conflicts/SKILL.md`
- **类型**: Model-invoked → Skill
- **修改**：无。自包含 15 行，无引用。

#### 16. research
- **来源**: `skills/engineering/research/SKILL.md`
- **类型**: Model-invoked → Skill
- **修改**：无。自包含 13 行，无引用。

#### 17. domain-modeling
- **来源**: `skills/engineering/domain-modeling/`（SKILL.md + ADR-FORMAT.md + CONTEXT-FORMAT.md）
- **类型**: Model-invoked → Skill
- **修改**：
  - 子文件引用为相对路径，Skill 格式完全支持（R9）
  - 第80-83行 slash command 引用修正（R8）：`/grill-with-docs` 保持不变（描述性上下文）；`Run /to-spec` → `Run /to-spec`（这些已是"建议用户运行"的句式，原文使用 "Want to create a spec first with `/to-spec`" 等措辞，本就是面向用户的建议句式）

#### 18. codebase-design
- **来源**: `skills/engineering/codebase-design/`（SKILL.md + DEEPENING.md + DESIGN-IT-TWICE.md）
- **类型**: Model-invoked → Skill
- **修改**：无。子文件引用保持不变（R9）。

#### 19. tdd
- **来源**: `skills/engineering/tdd/`（SKILL.md + mocking.md + tests.md）
- **类型**: Model-invoked → Skill
- **修改**：
  - 子文件引用保持不变（R9）
  - `code-review` skill 引用保持不变（R9：skill 引用 skill）

#### 20. diagnosing-bugs
- **来源**: `skills/engineering/diagnosing-bugs/`（SKILL.md + scripts/hitl-loop.template.sh）
- **类型**: Model-invoked → Skill
- **修改**：
  - 第134行：`hand off to the /improve-codebase-architecture skill` → `suggest the user run the /improve-codebase-architecture slash command`（R8）
  - `scripts/hitl-loop.template.sh` 保留（skill 支持脚本引用，R4）

#### 21. code-review
- **来源**: `skills/engineering/code-review/SKILL.md`
- **类型**: Model-invoked → Skill
- **修改**：
  - `run /setup-matt-pocock-skills if the issue tracker config file... is missing` → `tell the user to run /setup-matt-pocock-skills if...`（R8）
  - `carry the context via /handoff` → `tell the user to open one and carry the context via /handoff`（R8）
  - `then /implement in a new session` → `then tell the user to run /implement in a new session`（R8）

#### 22. prototype
- **来源**: `skills/engineering/prototype/`（SKILL.md + LOGIC.md + UI.md）
- **类型**: Model-invoked → Skill
- **修改**：无。子文件引用保持不变（R9）。无跨 skill/command 引用需要修正。

---

## 忽略项（明确不迁移）

| 来源 | 原因 |
|---|---|
| `skills/personal/edit-article` | R10 — personal |
| `skills/personal/obsidian-vault` | R10 — personal |
| `skills/in-progress/batch-grill-me` | R11 — in-progress |
| `skills/in-progress/claude-handoff` | R11 — in-progress |
| `skills/in-progress/loop-me` | R11 — in-progress |
| `skills/in-progress/setup-ts-deep-modules` | R11 — in-progress |
| `skills/in-progress/to-questionnaire` | R11 — in-progress |
| `skills/in-progress/wizard` | R11 — in-progress |
| `skills/in-progress/writing-beats` | R11 — in-progress |
| `skills/in-progress/writing-fragments` | R11 — in-progress |
| `skills/in-progress/writing-shape` | R11 — in-progress |
| `skills/deprecated/*` | R12 — deprecated |
| `skills/misc/git-guardrails-claude-code` | R13 — Claude Code 专用 |
| `skills/misc/setup-pre-commit` | R13 — 用户不需要 |
| `skills/misc/migrate-to-shoehorn` | R13 — 用户用不上 |
| `skills/misc/scaffold-exercises` | R13 — 非开发方向 |
| 所有 skill 的 `agents/openai.yaml` | R13 — Codex 元数据文件，Cline 系平台不使用 |

---

## 最终目录结构

```
roocode/
├── commands/
│   ├── ask-matt.md
│   ├── grill-me.md
│   ├── grill-with-docs.md
│   ├── handoff.md
│   ├── implement.md
│   ├── improve-codebase-architecture.md
│   ├── setup-matt-pocock-skills.md
│   ├── teach.md
│   ├── to-spec.md
│   ├── to-tickets.md
│   ├── triage.md
│   ├── wayfinder.md
│   └── writing-great-skills.md
├── migration-logs/
│   └── 2025-07-22_initial.md
└── skills/
    ├── codebase-design/
    │   ├── DEEPENING.md
    │   ├── DESIGN-IT-TWICE.md
    │   └── SKILL.md
    ├── code-review/
    │   └── SKILL.md
    ├── diagnosing-bugs/
    │   ├── SKILL.md
    │   └── scripts/
    │       └── hitl-loop.template.sh
    ├── domain-modeling/
    │   ├── ADR-FORMAT.md
    │   ├── CONTEXT-FORMAT.md
    │   └── SKILL.md
    ├── grilling/
    │   └── SKILL.md
    ├── prototype/
    │   ├── LOGIC.md
    │   ├── SKILL.md
    │   └── UI.md
    ├── research/
    │   └── SKILL.md
    ├── resolving-merge-conflicts/
    │   └── SKILL.md
    └── tdd/
        ├── mocking.md
        ├── SKILL.md
        └── tests.md
```
